/**
 * Componente de página 404 - Not Found
 * Exibida quando o usuário acessa uma rota que não existe
 * Definida como rota catch-all (*) no sistema de rotas
 */
const NotFound = () => {
    return <>
        {/* Conteúdo temporário - placeholder para desenvolvimento de página 404 customizada */}
        <p>Page NotFound</p>
    </>
}

// Exporta o componente como padrão
export default NotFound;